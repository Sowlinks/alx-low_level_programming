#include "main.h"

/**
 * mul- multiplication function
 * @a: first number
 * @b: second number
 * Descriptipn: a function that multiplies two numbers
 * Return: 0
 */
int mul(int a, int b)
{
	int product = a * b;
	return (product);
}
