#include <stdio.h>

/**
 * main - to be done later
 * Descriptipn: file to be edited later
 *
 * Return: 0
 */
int main(void)
{
	int first = 0;

	int second = 1;

	while (first < 10)
	{
		putchar(first);

		putchar(second);

		putchar(',');

		putchar(' ');

		first++;

		second++
	}

	return (0);
}
