#include <stdio.h>

/**
 * main - to be done later
 * Descriptipn: file to be edited later
 *
 * Return: 0
 */
int main(void)
{
	int i = 0;

	int j = 1;

	while (i < 10)
	{
		putchar(i);

		putchar(j);

		putchar(',');

		putchar(' ');

		i++;
	}

	return (0);
}
