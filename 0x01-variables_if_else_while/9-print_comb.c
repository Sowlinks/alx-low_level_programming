#include <stdio.h>

/**
 * main - to be done later
 * Descriptipn: file to be edited later
 *
 * Return: 0
 */
int main(void)
{
	int first = 0;

	while (first < 10)
	{
		putchar(first);

		putchar(',');

		putchar(' ');

		first++;

	}

	return (0);
}
