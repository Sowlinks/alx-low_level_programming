#include "main.h"

/**
 * _abs- computes absolute value
 * @n: n holds the number whose value is checked
 * Description: prints the absolute value of an integer
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return( n);
	}
	else
	{
		return('1' * n);
	}
}
