#include "main.h"

/**
 * add - add two numbers
 * @a: first number
 * @b: second number
 * Description: a function that adds two numbers
 * Return: 0
 */
int add(int a, int b)
{
	int sum_total = a + b;

	return (sum_total);
}
